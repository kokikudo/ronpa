import 'package:flutter/material.dart';

class RoomListScreen extends StatelessWidget {
  const RoomListScreen({Key? key}) : super(key: key);
  static const id = '/home/roomList';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
